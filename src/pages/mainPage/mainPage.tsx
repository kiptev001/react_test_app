import React, { ChangeEvent, DialogHTMLAttributes, useState } from 'react';

const MainPage = () => {
  const [progress, setProgress] = useState(0);

  return (
    <>
      <div>mainPage</div>
    </>
  );
};

export default MainPage;
